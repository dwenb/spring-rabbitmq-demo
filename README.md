#依赖要求

+ 最低spring框架版本5.1.x。
+ 最低Java客户端库amqp-client依赖的版本是5.4.0。

#项目说明

+ spring-boot-rabbitmq-config
+ spring-rabbitmq-java-config
+ spring-rabbitmq-quick-connection
+ spring-rabbitmq-xml-config 

是几种不同连接rabbitmq server的方式。
此项目运行的前提是rabbitmq server已经安装，且用默认的配置（项目中连接用到了很多默认用户名密码等参数）
